import type { Load } from '@sveltejs/kit';

// export const load: Load = async ({ fetch }) => {
// 	const film: Film = await (await fetch('/api/films/569094')).json();

// 	const searchedFilms: Film[] = await (await fetch('/api/films/search/wall-e')).json();

// 	return {
// 		film,
// 		searchedFilms
// 	};
// };
